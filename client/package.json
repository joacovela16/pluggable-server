{
  "name": "scala-plugin-client",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Joaquín Velázquez Camacho",
  "license": "MIT",
  "scripts": {
    "dev": "rimraf .cache && rimraf dist &&  parcel serve  src/index.html",
    "build": "rimraf .cache && rimraf dist && parcel build src/index.html",
    "prod-build": "rimraf dist && parcel build --public-url /app  src/index.html"
  },
  "browserslist": [
    "last 1 chrome versions"
  ],
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.12.0",
    "axios": "^0.19.0",
    "bulma": "^0.8.0",
    "svelte": "^3.16.7"
  },
  "devDependencies": {
    "node-sass": "^7.0.0",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-svelte": "^4.0.5",
    "rimraf": "^3.0.0",
    "sass": "^1.24.0",
    "svelte-preprocess": "^3.3.0"
  }
}
